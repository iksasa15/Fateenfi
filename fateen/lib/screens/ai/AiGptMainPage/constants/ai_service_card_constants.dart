import 'package:flutter/material.dart';

class AiServiceCardConstants {
  // ألوان البطاقة
  static const Color cardBorderColor = Color(0xFFE3E0F8);
  static const Color titleColor = Color(0xFF221291);
  static const Color arrowBackgroundColor = Color(0xFFF6F4FF);
  static const Color arrowColor = Color(0xFF221291);

  // أحجام البطاقة
  static const double cardBorderRadius = 16.0;
  static const double cardBorderWidth = 1.5;
  static const double cardPadding = 16.0;

  // أحجام الأيقونة
  static const double iconContainerSize = 60.0;
  static const double iconBorderRadius = 12.0;
  static const double iconSize = 30.0;
  static const double iconBackgroundOpacity = 0.1;

  // أحجام السهم
  static const double arrowContainerSize = 24.0;
  static const double arrowBorderRadius = 6.0;
  static const double arrowSize = 14.0;

  // المسافات
  static const double contentSpacing = 16.0;
  static const double textSpacing = 4.0;

  // خصائص الظل
  static const double shadowOpacity = 0.05;
  static const double shadowSpreadRadius = 1.0;
  static const double shadowBlurRadius = 2.0;

  // خصائص النص
  static const double titleFontSize = 16.0;
  static const double descriptionFontSize = 13.0;
  static const int descriptionMaxLines = 2;
}
